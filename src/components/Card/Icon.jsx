import React from "react";

const Icon = ({ children }) => {
  return (
    <div className="mb-8 bg-indigo-600 rounded-full text-6xl p-8 object-center object-cover ">
      {children}
    </div>
  );
};

export default Icon;
